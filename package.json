{
  "name": "transit-planner",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "check": "npm --workspace web run check",
    "test": "npm --workspace web run test",
    "validate:routes": "npm --workspace web run validate:routes",
    "lint": "npm --workspace web run lint",
    "typecheck": "npm --workspace web run typecheck"
  },
  "packageManager": "npm@10.9.2",
  "dependencies": {
    "@sentry/nextjs": "^10.49.0",
    "mixpanel-browser": "^2.78.0",
    "next": "~15.2.8"
  },
  "optionalDependencies": {
    "@tailwindcss/oxide-linux-x64-gnu": "^4.2.1",
    "lightningcss-linux-x64-gnu": "^1.31.1"
  }
}
